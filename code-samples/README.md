# Code Samples (Selected Excerpts)

This folder contains short excerpts that highlight engineering decisions and patterns.

Guidelines:

- Keep each sample focused on one concept.
- Do not include API keys, tokens, `.env`, credentials, internal URLs, or personal data.
- Prefer interfaces and contracts (types, config shape, error handling) over full implementations.

