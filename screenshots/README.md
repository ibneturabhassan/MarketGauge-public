# Screenshots

Drop curated, publication-safe screenshots here.

Checklist before adding:

- No API keys, tokens, or `.env` content visible
- No local filesystem paths visible
- No browser extensions or personal account info visible
- No internal URLs, IPs, or deployment hostnames visible

Suggested shots:

- Dashboard overview (latest snapshot)
- Score detail page (history + component breakdown)
- Currency strength table/board
- FX pair view (if enabled in UI)

